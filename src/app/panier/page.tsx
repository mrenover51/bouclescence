"use client";
import Image from "next/image";
import Link from "next/link";
import { Minus, Plus, ShoppingBag } from "lucide-react";
import { useCart } from "@/lib/cart-store";
import { formatPrice } from "@/lib/products";
export default function CartPage() { const { items, remove, setQuantity } = useCart(); const subtotal = items.reduce((sum,item)=>sum+item.price*item.quantity,0); const shipping = subtotal >= 80 ? 0 : 5.9; if (!items.length) return <div className="shell grid min-h-[60svh] place-content-center py-20 text-center"><ShoppingBag className="mx-auto" strokeWidth={1.2}/><h1 className="display mt-6 text-5xl">Votre panier est vide</h1><Link href="/boutique" className="mt-7 text-xs font-bold uppercase tracking-[.16em] underline underline-offset-6">Découvrir la boutique</Link></div>; return <div className="shell py-14 sm:py-20"><h1 className="display text-5xl sm:text-6xl">Votre panier</h1><div className="mt-10 grid gap-12 lg:grid-cols-[1fr_24rem]"><div className="divide-y divide-line border-y border-line">{items.map(item=><div className="grid grid-cols-[6rem_1fr] gap-5 py-6 sm:grid-cols-[8rem_1fr]" key={item.slug}><div className="relative aspect-[4/5]"><Image src={item.image} alt="" fill className="object-cover" sizes="128px"/></div><div className="flex flex-col justify-between"><div className="flex justify-between gap-3"><div><h2 className="display text-2xl">{item.name}</h2><p className="mt-1 text-sm text-muted">{formatPrice(item.price)}</p></div><button onClick={()=>remove(item.slug)} className="self-start text-xs underline">Retirer</button></div><div className="flex w-fit items-center border border-line"><button className="p-2" onClick={()=>setQuantity(item.slug,item.quantity-1)}><Minus size={14}/></button><span className="min-w-9 text-center">{item.quantity}</span><button className="p-2" onClick={()=>setQuantity(item.slug,item.quantity+1)}><Plus size={14}/></button></div></div></div>)}</div><aside className="h-fit bg-ivory p-7"><h2 className="display text-3xl">Récapitulatif</h2><div className="mt-6 space-y-4 border-b border-line pb-6 text-sm"><div className="flex justify-between"><span>Sous-total</span><span>{formatPrice(subtotal)}</span></div><div className="flex justify-between"><span>Livraison</span><span>{shipping ? formatPrice(shipping) : "Offerte"}</span></div></div><div className="flex justify-between py-6 text-lg font-bold"><span>Total</span><span>{formatPrice(subtotal+shipping)}</span></div><Link href="/checkout" className="flex min-h-14 items-center justify-center bg-wine text-xs font-bold uppercase tracking-[.16em] text-white">Passer commande</Link><p className="mt-4 text-center text-xs leading-5 text-muted">Paiement sécurisé — activation Stripe prévue au Sprint 3.</p></aside></div></div>; }
