import { ProductCard } from "@/components/shop/product-card";
import { products } from "@/lib/products";
export default async function CollectionPage({ params }: PageProps<"/collections/[slug]">) { await params; return <div className="shell py-16 sm:py-24"><header className="max-w-2xl"><p className="eyebrow text-wine">Collection</p><h1 className="display mt-3 text-6xl">Éclat nacré</h1><p className="mt-5 leading-8 text-muted">Perles naturelles et lumière dorée : une collection douce, organique et singulière.</p></header><div className="mt-14 grid gap-8 sm:grid-cols-2 lg:grid-cols-3">{products.filter(p => p.collection === "Éclat nacré").map(product => <ProductCard key={product.slug} product={product}/>)}</div></div>; }
