import type { Metadata } from "next";import { pageMetadata } from "@/lib/seo";export const metadata:Metadata=pageMetadata({title:"À propos de Bouclescence",description:"Découvrez l’univers de Bouclescence, ses inspirations et sa vision de bijoux féminins délicats et singuliers.",path:"/a-propos"});export default function Layout({children}:{children:React.ReactNode}){return children}
