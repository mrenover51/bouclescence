import Image from "next/image";
import Link from "next/link";
import { formatPrice, type Product } from "@/lib/products";
export function ProductCard({ product, preload = false }: { product: Product; preload?: boolean }) { const alt=product.gallery?.find(image=>image.isPrimary)?.alt||product.name;return <article className="group min-w-0"><Link href={`/produit/${product.slug}`} className="relative block aspect-[4/5] overflow-hidden bg-ivory"><Image src={product.image} alt={alt} fill preload={preload} sizes="(max-width: 767px) calc(50vw - 22px), (max-width: 1279px) calc(33vw - 24px), 330px" className="object-cover transition duration-700 ease-out group-hover:scale-[1.025]" />{product.badge && <span className="absolute left-2 top-2 bg-paper/95 px-2 py-1.5 text-[.55rem] font-bold uppercase tracking-[.14em] sm:left-3 sm:top-3 sm:px-3 sm:py-2 sm:text-[.6rem]">{product.badge}</span>}</Link><div className="mt-3 sm:mt-4"><p className="truncate text-[.6rem] uppercase tracking-[.14em] text-muted sm:text-[.65rem] sm:tracking-[.17em]">{product.collection}</p><div className="mt-1 flex items-start justify-between gap-2"><h3 className="display min-w-0 text-xl leading-tight sm:text-2xl"><Link href={`/produit/${product.slug}`}>{product.name}</Link></h3><p className="shrink-0 pt-1 text-xs whitespace-nowrap sm:text-sm">{formatPrice(product.price)}</p></div></div></article>; }
