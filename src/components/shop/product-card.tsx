import Image from "next/image";
import Link from "next/link";
import { formatPrice, type Product } from "@/lib/products";
export function ProductCard({ product, priority = false }: { product: Product; priority?: boolean }) { const alt=product.gallery?.find(image=>image.isPrimary)?.alt||product.name;return <article className="group"><Link href={`/produit/${product.slug}`} className="relative block aspect-[4/5] overflow-hidden bg-ivory"><Image src={product.image} alt={alt} fill priority={priority} sizes="(max-width: 768px) 85vw, 32vw" className="object-cover transition duration-700 ease-out group-hover:scale-[1.025]" />{product.badge && <span className="absolute left-4 top-4 bg-paper/95 px-3 py-2 text-[.62rem] font-bold uppercase tracking-[.16em]">{product.badge}</span>}</Link><div className="mt-4 flex items-start justify-between gap-3"><div><p className="text-[.67rem] uppercase tracking-[.17em] text-muted">{product.collection}</p><h3 className="display mt-1 text-2xl"><Link href={`/produit/${product.slug}`}>{product.name}</Link></h3></div><p className="pt-5 text-sm whitespace-nowrap">{formatPrice(product.price)}</p></div></article>; }
