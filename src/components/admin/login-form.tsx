"use client";
import { useActionState } from "react";
import { login, type LoginState } from "@/app/(auth)/admin/login/actions";
export function LoginForm(){const [state,action,pending]=useActionState<LoginState,FormData>(login,null);return <form action={action} className="mt-8 space-y-5"><div><label className="text-xs font-bold uppercase tracking-[.14em]" htmlFor="email">E-mail</label><input className="mt-2 min-h-12 w-full border border-line bg-paper px-4 outline-none focus:border-wine" id="email" name="email" type="email" autoComplete="email" required/></div><div><label className="text-xs font-bold uppercase tracking-[.14em]" htmlFor="password">Mot de passe</label><input className="mt-2 min-h-12 w-full border border-line bg-paper px-4 outline-none focus:border-wine" id="password" name="password" type="password" autoComplete="current-password" minLength={8} required/></div>{state?.error&&<p role="alert" className="bg-[#f6e5e5] p-3 text-sm text-wine">{state.error}</p>}<button disabled={pending} className="min-h-13 w-full bg-wine px-6 text-xs font-bold uppercase tracking-[.16em] text-white disabled:opacity-50">{pending?"Connexion…":"Se connecter"}</button></form>}
