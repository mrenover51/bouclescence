"use client";
import { useState } from "react";
const field="mt-2 min-h-12 w-full border border-line bg-white px-4 text-sm outline-none focus:border-wine";
export function SeoFields({defaultTitle="",defaultDescription="",fallbackTitle,fallbackDescription,path,titleName="seo_title",descriptionName="seo_description"}:{defaultTitle?:string;defaultDescription?:string;fallbackTitle:string;fallbackDescription:string;path:string;titleName?:string;descriptionName?:string}){const [title,setTitle]=useState(defaultTitle);const [description,setDescription]=useState(defaultDescription);return <section className="bg-paper p-6"><h2 className="display text-2xl">Référencement</h2><div className="mt-5 grid gap-5"><label className="text-sm font-semibold">Titre SEO <span className={title.length>60?"text-wine":"font-normal text-muted"}>{title.length} / ~60 caractères</span><input className={field} name={titleName} value={title} onChange={event=>setTitle(event.target.value)}/><span className="mt-1 block text-xs text-muted">Laissez vide pour utiliser le titre automatique. {title.length>60&&"Le titre risque d’être tronqué dans Google."}</span></label><label className="text-sm font-semibold">Meta description <span className={description.length>155?"text-wine":"font-normal text-muted"}>{description.length} / ~155 caractères</span><textarea className={`${field} min-h-24 py-3`} name={descriptionName} value={description} onChange={event=>setDescription(event.target.value)}/>{description.length>155&&<span className="mt-1 block text-xs text-wine">La description risque d’être tronquée dans Google.</span>}</label></div><div className="mt-6 border border-line bg-white p-5" aria-label="Prévisualisation Google"><p className="text-xs text-muted">Aperçu Google</p><p className="mt-3 text-lg text-[#1a0dab]">{title||fallbackTitle}</p><p className="mt-1 break-all text-sm text-[#188038]">{process.env.NEXT_PUBLIC_SITE_URL||"https://www.DOMAIN-FUTUR.fr"}{path}</p><p className="mt-1 text-sm leading-5 text-[#4d5156]">{description||fallbackDescription}</p></div></section>}
